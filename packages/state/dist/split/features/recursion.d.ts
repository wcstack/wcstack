import { I as IStateFeature } from '../chunks/features.js';

/**
 * features/recursion.ts — `$recursion` と `**`（@wcstack/state/features/recursion）。
 */

declare const recursion: IStateFeature;

export { recursion as default, recursion };
