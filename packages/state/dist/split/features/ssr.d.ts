import { I as IStateFeature } from '../chunks/features.js';

/**
 * features/ssr.ts — サーバー描画とハイドレーション（@wcstack/state/features/ssr）。
 */

declare const ssr: IStateFeature;

export { ssr as default, ssr };
