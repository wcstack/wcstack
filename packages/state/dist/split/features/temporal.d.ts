import { I as IStateFeature } from '../chunks/features.js';

/**
 * features/temporal.ts — `$watch` / `$scan` / `$streams`（@wcstack/state/features/temporal）。
 */

declare const temporal: IStateFeature;

export { temporal as default, temporal };
