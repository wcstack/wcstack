// Setup file for Vitest. The signal core is environment-agnostic; the bindNode /
// integration suites use happy-dom's EventTarget + DOM and a mocked global fetch
// installed per-test. Nothing global is required here.
