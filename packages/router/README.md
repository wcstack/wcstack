# @wcstack/router

Performs SPA routing with declarative definitions using custom elements.

## Features
- Declarative routing definitions using custom elements
- Layout definitions can be mixed into routing definitions
- Layouts can use slot assignment with Light DOM
- Supports the Navigation API
- Simple parameter binding support
- Global fallback support
- Zero config
- Zero dependencies
- Buildless
