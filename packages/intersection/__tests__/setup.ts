// Setup file for Vitest.
//
// happy-dom does not implement (a controllable) IntersectionObserver, so each
// test installs its own mock via the helpers in mocks.ts. This file is
// intentionally minimal — see mocks.ts for the controllable fake.
